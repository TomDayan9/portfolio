import { Container, Typography } from '@mui/material';

const Home = () => (
  <Container>
    <Typography variant="h2" gutterBottom>
      Welcome to My Portfolio
    </Typography>
    <Typography variant="body1">
      I'm a developer specializing in React, TypeScript, and backend development.
    </Typography>
  </Container>
);

export default Home;
